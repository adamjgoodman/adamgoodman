var partial = '<%= j render partial: "#{@partial}" %>'
$( '#content' ).fadeOut( 'slow' ).queue(function() {
    $( '#content' ).html( partial ).hide().fadeIn( 'slow' );
    $( '#nav' ).html( '<%= j render partial: "nav" %>' )
    $( this ).dequeue();
});
